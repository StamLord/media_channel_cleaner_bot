# Database that stores medie-only channels for persistence
channels.db

# Node modules directory
/node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific files
.DS_Store       # macOS
Thumbs.db       # Windows

# Environment files
.env
.env.local
.env.development
.env.test
.env.production

# Package-lock file or yarn.lock (optional, but can be excluded if you use `package-lock.json` with version control)
# .npmrc         # npm config file (optional)
# .yarn.lock     # Yarn lockfile (optional)

# Build directories
/dist/
/build/

# IDE or editor files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Operating system files (ignore those that might be created by OS)
.DS_Store
Thumbs.db

# Coverage reports
/coverage/

# Temporary files or build artifacts
*.tgz
*.gz

# npm cache
.npm/
.yarn/
